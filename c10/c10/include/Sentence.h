#ifndef SENTENCE_H
#define SENTENCE_H
#include <vector>
#include <string>
using namespace std;


class Sentence
{
    public:
        Sentence(vector<string>, vector<string>, vector<string>, vector<string>);
        virtual ~Sentence();
    protected:
    private:
};

#endif // SENTENCE_H
